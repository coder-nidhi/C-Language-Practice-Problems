#include<stdio.h>   //syntax
#include<conio.h>
int main ()
{
    int numArray[10], i;    //input in array for using loop
    for(i=0; i<10; i++)
    {
        printf("%d\n",i+1);     //display
       
    }
    return 0;
}
